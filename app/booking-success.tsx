import { router, useLocalSearchParams } from 'expo-router';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { ScreenContainer } from '@/components/screen-container';
import { IconSymbol } from '@/components/ui/icon-symbol';
import { PrimaryButton } from '@/components/trip-ui';
import { trips } from '@/lib/demo-data';

export default function BookingSuccessScreen() { const { mode } = useLocalSearchParams<{ mode?: string }>(); const trip = trips[0]; return <ScreenContainer edges={['top', 'left', 'right', 'bottom']} className="bg-background" safeAreaClassName="bg-background"><ScrollView contentContainerStyle={{ padding: 20, paddingBottom: 40 }}><Pressable onPress={() => router.replace('/(tabs)')} className="self-end"><IconSymbol name="xmark" size={22} color="#617064" /></Pressable><View className="mt-8 items-center"><View className="h-20 w-20 items-center justify-center rounded-full bg-accent"><IconSymbol name="checkmark.circle.fill" size={42} color="#2D5A27" /></View><Text className="mt-6 text-center font-heading text-3xl font-bold text-foreground">{mode === 'private' ? 'Brief terkirim.' : 'Booking tercatat.'}</Text><Text className="mt-2 max-w-[310px] text-center font-body text-sm leading-5 text-muted">{mode === 'private' ? 'Tim kami akan menghubungi kamu untuk menyusun detail private trip.' : 'Simpan kode ini. Detail pembayaran dan update perjalanan akan dikirim ke WhatsApp.'}</Text></View><View className="mt-8 rounded-3xl bg-primary p-5"><Text className="font-body text-xs font-bold uppercase tracking-widest text-accent">KODE BOOKING</Text><Text className="mt-2 font-body text-3xl font-extrabold tracking-[5px] text-white">OR-2608</Text><View className="mt-5 h-px bg-white/20" /><Text className="mt-4 font-body text-xs text-white/70">Status</Text><Text className="mt-1 font-body text-sm font-extrabold text-white">Menunggu konfirmasi pembayaran</Text></View>{mode !== 'private' ? <View className="mt-5 rounded-3xl border border-border bg-surface p-4"><Text className="font-heading text-lg font-bold text-foreground">{trip.title}</Text><Text className="mt-1 font-body text-xs text-muted">14–15 Sep 2026 · 1 peserta · {trip.location}</Text><View className="mt-4 flex-row items-center"><IconSymbol name="bell.fill" size={17} color="#2D5A27" /><Text className="ml-2 flex-1 font-body text-xs leading-5 text-muted">Pengingat briefing akan dikirim H-7 sebelum keberangkatan.</Text></View></View> : null}<View className="mt-6"><PrimaryButton label="Kembali ke beranda" onPress={() => router.replace('/(tabs)')} /></View><Pressable onPress={() => router.push('/private-trip')} className="mt-4 items-center"><Text className="font-body text-xs font-extrabold text-primary">Butuh bantuan? Hubungi tim Or.Trip</Text></Pressable></ScrollView></ScreenContainer>; }
