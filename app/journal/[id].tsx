import { Image } from 'expo-image';
import { router, useLocalSearchParams } from 'expo-router';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { ScreenContainer } from '@/components/screen-container';
import { IconSymbol } from '@/components/ui/icon-symbol';
import { journals } from '@/lib/demo-data';

export default function JournalDetailScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const article = journals.find((item) => item.id === id) ?? journals[0]; return <ScreenContainer edges={['top', 'left', 'right', 'bottom']} className="bg-background" safeAreaClassName="bg-background"><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 40 }}><View className="relative"><Image source={{ uri: article.image }} contentFit="cover" style={{ width: '100%', height: 270 }} /><Pressable onPress={() => router.back()} className="absolute left-5 top-4 h-10 w-10 items-center justify-center rounded-full bg-white/90"><IconSymbol name="arrow.left" size={20} color="#1A251B" /></Pressable></View><View className="px-5 pt-6"><Text className="font-body text-xs font-extrabold uppercase tracking-widest text-primary">{article.category} · {article.readTime}</Text><Text className="mt-2 font-heading text-3xl font-bold leading-9 text-foreground">{article.title}</Text><Text className="mt-4 font-body text-base leading-7 text-muted">{article.excerpt}</Text><Text className="mt-6 font-heading text-xl font-bold text-foreground">Mulai dengan ritme yang tepat</Text><Text className="mt-3 font-body text-sm leading-6 text-muted">Perjalanan ke alam bukan tentang siapa yang paling cepat sampai. Yang penting adalah memahami kondisi diri, membawa perlengkapan secukupnya, dan memberi ruang untuk menikmati detail kecil di sepanjang jalur.</Text><Text className="mt-5 font-body text-sm leading-6 text-muted">Sebelum berangkat, cek cuaca dan status kawasan. Bagikan rencana perjalanan kepada orang terdekat, simpan kontak guide, serta siapkan alternatif bila kondisi berubah. Dengan persiapan yang tenang, pengalaman di gunung terasa lebih aman dan lebih berkesan.</Text><View className="mt-7 rounded-3xl bg-neutral p-4"><View className="flex-row items-center"><IconSymbol name="sparkles" size={18} color="#2D5A27" /><Text className="ml-2 font-body text-sm font-extrabold text-foreground">Catatan Or.Trip</Text></View><Text className="mt-2 font-body text-xs leading-5 text-muted">Selalu ikuti aturan resmi kawasan dan jangan ragu menghubungi tim jika kamu butuh rekomendasi trip sesuai level pengalaman.</Text></View></View></ScrollView></ScreenContainer>; }
