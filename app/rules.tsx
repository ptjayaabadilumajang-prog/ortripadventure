import { Pressable, ScrollView, Text, View } from 'react-native';
import { router } from 'expo-router';
import { ScreenContainer } from '@/components/screen-container';
import { IconSymbol } from '@/components/ui/icon-symbol';
import { rules } from '@/lib/demo-data';

export default function RulesScreen() { return <ScreenContainer edges={['top', 'left', 'right', 'bottom']} className="bg-background" safeAreaClassName="bg-background"><ScrollView contentContainerStyle={{ padding: 20, paddingBottom: 36 }}><Pressable onPress={() => router.back()} className="mb-5 flex-row items-center"><IconSymbol name="arrow.left" size={18} color="#2D5A27" /><Text className="ml-2 font-body text-sm font-bold text-primary">Kembali</Text></Pressable><Text className="font-body text-xs font-bold uppercase tracking-widest text-primary">SAFETY GUIDE</Text><Text className="mt-2 font-heading text-3xl font-bold text-foreground">Naik dengan bijak.</Text><Text className="mt-2 font-body text-sm leading-5 text-muted">Panduan ringkas untuk membantu perjalananmu tetap aman, nyaman, dan menghormati kawasan.</Text><View className="mt-6 rounded-3xl bg-primary p-5"><IconSymbol name="shield.fill" size={25} color="#A3C9A8" /><Text className="mt-3 font-heading text-xl font-bold text-white">Semeru & Bromo Tengger Semeru</Text><Text className="mt-2 font-body text-xs leading-5 text-white/80">Peraturan kawasan dapat berubah. Selalu cek pengumuman resmi dan ikuti arahan petugas serta guide.</Text></View><View className="mt-6">{rules.map((rule) => <View key={rule.title} className="mb-3 rounded-3xl border border-border bg-surface p-4"><View className="flex-row items-start"><View className={`h-9 w-9 items-center justify-center rounded-xl ${rule.tone === 'warning' ? 'bg-[#F5E6C8]' : rule.tone === 'success' ? 'bg-accent' : 'bg-neutral'}`}><IconSymbol name="checkmark.circle.fill" size={18} color="#2D5A27" /></View><View className="ml-3 flex-1"><Text className="font-body text-sm font-extrabold leading-5 text-foreground">{rule.title}</Text><Text className="mt-2 font-body text-xs leading-5 text-muted">{rule.body}</Text></View></View></View>)}</View><Text className="mt-5 font-heading text-xl font-bold text-foreground">Sebelum berangkat</Text>{['Cek kesehatan dan perlengkapan pribadi', 'Simpan kontak guide dan emergency contact', 'Pahami cuaca dan status jalur', 'Hormati warga, alam, dan sesama pendaki'].map((item) => <View key={item} className="mt-3 flex-row items-center"><IconSymbol name="checkmark.circle.fill" size={16} color="#2D5A27" /><Text className="ml-2 font-body text-sm text-foreground">{item}</Text></View>)}</ScrollView></ScreenContainer>; }
